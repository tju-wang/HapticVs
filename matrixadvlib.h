#ifndef _MATRIXADVLIB_H
#define _MATRIXADVLIB_H
#include "matrixlib.h"

// void LUdecomposition(matrix* a, matrix** l, matrix** u);
// double determinantMatrix(matrix* a);
// matrix* inverseMatrix(matrix* a);
// matrix* solver(matrix* a, matrix* b);

#endif
